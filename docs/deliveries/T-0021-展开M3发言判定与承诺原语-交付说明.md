# T-0021 · 展开 M3：发言判定与承诺原语 · 交付说明

## 一、交付内容

本次按 owner 确认的方案 A 展开 M3：先交付 T-0041 真实 Agent 运行时，再并行进入 T-0016 发言判定与 T-0017 待答项，之后分别推进 T-0020 人来让位与 T-0018 → T-0019 承诺 / 持球链。

交付物：

- `docs/plans/2026-08-12-t0021-expand-m3.md`：六项 shaped 合同、依赖、失败反例、原语映射与 Buzz 翻译。
- Blueprint 中 T-0016～T-0020、T-0041 的 maturity / criteria / dependencies，以及 horizon M3。
- 依赖图的唯一首个可认领任务为 T-0041；T-0041 未 verified 前，其余五项均不可认领。

## 二、T-0021 七条验收标准与证据

1. **全部 seed→shaped：** T-0016、T-0017、T-0018、T-0019、T-0020、T-0041 均写入闭合验收标准；分别为 7 / 6 / 6 / 6 / 6 / 7 条，共 38 条。
2. **展开后可继续工作：** 方案 A 干跑的派生视图精确显示 `可认领: T-0041`，不是空池，也没有提前解锁后续任务。
3. **推进范围一致：** Blueprint horizon 从 M2 推进到 M3；六次任务结构更新与一次 horizon 更新都通过 `gbp.py` 原子写入并进入 timeline，kind 为 structure / 展开 M3。
4. **可证伪：** 每个任务都有具体状态、字段、数值边界、错误 / 并发 / 重放反例；没有以“做好、合理、完善、优化”代替合同。
5. **原语或理由完整：** T-0041 / T-0016 / T-0017 / T-0019 / T-0020 均标注 PRD 2.3 原语；T-0018 明确为承诺层最小事实，不新增社交原语，并解释与 GBP 的隔离边界。
6. **来源与拆分有解释：** 设计文档逐项记录原 seed 来源、T-0041 为何由 Alpha-first 新增、路由 / 执行 / 承诺 / 调度为何拆开，以及 mandatory @、唯一 holder、一次性工具确认等展开时发现的缺口。
7. **结构校验：** 最终以 `gbp.py check --links` 校验依赖、timeline、文档链接及派生视图；交付前要求输出 `✓ 无违规`。

## 三、Buzz 参照、翻译与偏离

- **参照：** EventQueue 的 room-scoped 有界串行、PoolLifecycle 的 attempt / stale-result fence、relay 的持久化后 ACK 与游标恢复、PromptContext 的模型 / 工具 / 凭据注入。
- **翻译：** Rust channel / Agent pool 对应 TypeScript room scheduler、AbortSignal、attempt-branded execution；Buzz mention 对应 T-0040 closed event 与 server-minted capability；通用 prompt 入口拆为摘要 RouterProvider 与完整上下文 AgentRuntimeProvider。
- **偏离：** 不引入 Nostr、公钥事件、community、多租户、Redis、Git / workflow / media；human 与 Agent 原语保持分离；human durable message 是硬 preemption fence；completion / dead-letter / confirmation / compensation 复用 T-0040 权威事件、outbox 与恢复管线。

## 四、解锁、验收与后续边界

验收时请重点核对：

- T-0041 是唯一新可认领任务，仍硬依赖已 verified 的 T-0040 与 T-0013。
- T-0041 verified 后只并行解锁 T-0016、T-0017；之后为 T-0016→T-0020 与 T-0017→T-0018→T-0019。
- 显式 `@agent` 是 mandatory invocation intent，与 RouterProvider 候选去重，不能零执行或双执行。
- OpenItem / LightTask 状态到唯一 holder 的映射闭合；todo / verified LightTask 不产球，delivered 必须先持久化唯一 verifierActorId。
- M3 只交付 room-scoped NeedsActionProjection / ReminderCandidate；跨群收件箱和通知留给 M4，真实 GBP 写入留给 M5。

本任务交付后只进入 `delivered` 并把验收球交给 `@lionel`；不会代替 owner 勾选通过，也不会在同一轮继续认领 T-0041。

# Dao 项目宪法

本文件适用于仓库根目录及其全部子目录。任何更深层级的 `AGENTS.md` 只能补充局部实现规则，不能削弱本文件中的权威关系和 UI 约束。

## 权威来源与优先级

实现前必须按问题类型读取对应的权威来源：

1. [产品 PRD](docs/reconstruction/2026-08-agent群聊协作模式-prd.reconstructed.md)定义产品语义、权限、不变量和 Requirement；
2. [协议规范](docs/protocols/)与已批准的 feature spec 定义服务端事实、命令、ACK、事件、错误和恢复合同；
3. [UI / 交互设计基线](docs/design/README.md)及其链接的正式审阅稿，定义 Desktop 信息架构、组件、状态、操作和视觉；
4. 生产代码与测试用于证明实现，不得反向改写以上权威输入。

发生冲突时，停止猜测和自由发挥：指出冲突涉及的具体 Requirement、协议或设计状态，并等待 owner 明确裁决。不得用现有代码、第三方项目或原型效果覆盖本项目的权威产品与设计合同。

## UI / 交互实现的不可协商规则

**凡是新增、修改或重构 UI 界面与交互，必须依据项目内权威 UI / 交互设计稿实现。** 权威入口是 [docs/design/README.md](docs/design/README.md)，正式评审以该入口当前指向的自包含审阅稿和 Requirement 覆盖矩阵为准。截图、口头描述、历史实现、临时原型和参考项目都不能替代该设计基线。

开始编码前，Agent 必须先完成并在计划、spec 或交付说明中保留以下映射：

- 对应的 `REQ-*` 与 FT 编号；
- 对应的设计旅程 `J-01`～`J-07`、设计稿分区、组件和状态；
- 每个可见状态的权威来源：local transient、server ACK、stable event 或 projection；
- 适用的 loading、empty、401 / 403 / 409 / 410 / 429 / 503、offline、repair 和重试分支；
- 键盘操作、焦点、非颜色识别、可访问通告、缩放与 reduced motion 要求；
- 与设计稿的偏离；默认必须写“无”。如确需偏离，必须记录原因、owner 批准和替代验收证据后才能实现。

实现时还必须遵守：

- 视觉层级、布局、组件关系、文案、交互顺序和状态转换均以设计稿为准，不得只实现 happy path；
- 设计稿中的演示按钮、假数据和纯展示效果若没有协议命令、ACK、事件或 projection 支撑，只能视为 `prototype-only`，不得伪装成已生效的业务状态；
- 客户端只提交意图并呈现权威结果，不得把本地 UI 状态当作权限、持久化成功或服务端事实；
- 测试必须覆盖本次触及的设计状态和关键可访问性合同；视觉或交互验收应能回指具体设计旅程与状态，而不是以“与现有页面一致”代替证据；
- 如果设计稿未覆盖所需状态，先补齐或获得明确设计决策，不得自行创造长期产品行为。

## Buzz 参考项目的使用规则

本仓库的 [Buzz 本地副本](buzz/)仅用于聊天 Agent 相关设计与实现的定向参考。首次查阅前先读：

- [Buzz 参考项目介绍](docs/references/buzz-reference/README.md)
- [Buzz 代码地图](docs/references/buzz-reference/CODE_MAP.md)

Buzz 的地位始终低于本项目的 PRD、协议和 UI / 交互设计稿：

- 可以借鉴组件拆分、消息时间线、Agent 点名、执行活动面板、状态管理、进程生命周期和测试组织方式；
- 不得因为 Buzz 已有某个页面、交互或协议，就把它直接当成本项目需求；
- 不得把 Buzz 的 Nostr、community、多租户、relay 或事件 kind 语义无条件移植到本项目；
- 参考时先通过代码地图定位最小文件集合，再阅读调用链和相邻测试，避免全量复制或从单个组件猜测完整行为；
- 如直接移植或实质改写 Buzz 代码，必须遵守其 Apache-2.0 许可证并保留必要的来源与版权说明；优先借鉴模式并按本项目合同重新实现。

## 保持文档真实

若 Buzz 本地副本更新到新提交，或其关键入口发生变化，必须同步更新介绍文档中的版本信息和代码地图。若本项目的 PRD、协议或设计基线入口变更，应在同一次变更中更新本文件，避免 Agent 继续读取过期权威来源。

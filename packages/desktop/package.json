{
  "name": "@native-im/desktop",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.json && node scripts/build-preload.mjs && node scripts/copy-renderer.mjs",
    "smoke": "node scripts/smoke-electron.mjs",
    "test": "vitest run --config vitest.config.ts",
    "start": "electron dist/main.js"
  },
  "dependencies": {
    "@native-im/core": "workspace:*"
  },
  "devDependencies": {
    "@types/ws": "^8.5.14",
    "electron": "43.4.1",
    "esbuild": "0.28.1",
    "ws": "^8.21.2"
  }
}
